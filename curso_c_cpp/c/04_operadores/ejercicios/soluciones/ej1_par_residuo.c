/* Ejercicio 1 - Residuo entre 2 (0 = par, 1 = impar). */
#include <stdio.h>

int main(void) {
    int n;
    printf("Numero: ");
    scanf("%d", &n);
    printf("n %% 2 = %d\n", n % 2);
    return 0;
}
