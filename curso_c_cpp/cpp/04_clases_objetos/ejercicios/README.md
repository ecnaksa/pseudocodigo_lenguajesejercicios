# Ejercicios — Lección 04 (C++)

1. **Clase Circulo.** Crea `Circulo` con atributo `radio` (public) y métodos `area()` y
   `circunferencia()`. → [`soluciones/ej1_circulo.cpp`](soluciones/ej1_circulo.cpp)

2. **Clase Contador.** Con atributo `private int valor` y métodos `incrementar()`,
   `reiniciar()` y `obtener()`. → [`soluciones/ej2_contador.cpp`](soluciones/ej2_contador.cpp)

3. **Persona con getters/setters.** `Persona` con `nombre` y `edad` privados, y getters/setters
   (el `setEdad` no debe aceptar edades negativas).
   → [`soluciones/ej3_persona.cpp`](soluciones/ej3_persona.cpp)

4. **Reto — clase Termometro.** Atributo `private double celsius`, con `set(double)` y métodos
   `getCelsius()` y `getFahrenheit()`. → [`soluciones/ej4_termometro.cpp`](soluciones/ej4_termometro.cpp)
