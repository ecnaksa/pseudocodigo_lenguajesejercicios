# Ejercicios — Lección 07 (C++)

1. **Vector2D con resta.** Amplía el `Vector2D` del ejemplo con `operator-` y con
   `operator*` para el producto por un escalar (`v * 2.0`).
   → [`soluciones/ej1_vector.cpp`](soluciones/ej1_vector.cpp)

2. **Dinero.** Clase `Dinero` (entero de centavos) con `operator+`, `operator<` y `operator<<`
   (que muestre `$X.XX`). → [`soluciones/ej2_dinero.cpp`](soluciones/ej2_dinero.cpp)

3. **Reto — Complejo.** Clase `Complejo` (parte real e imaginaria) con `operator+`, `operator*`
   (multiplicación de complejos) y `operator<<` (formato `a + bi`).
   → [`soluciones/ej3_complejo.cpp`](soluciones/ej3_complejo.cpp)
