# Ejercicios — Lección 08 (C++)

1. **`minimo` genérico.** Escribe una plantilla `T minimo(T a, T b)` y pruébala con `int`,
   `double` y `string`. → [`soluciones/ej1_minimo.cpp`](soluciones/ej1_minimo.cpp)

2. **`intercambiar` genérico.** Plantilla `void intercambiar(T &a, T &b)` que sirva para cualquier
   tipo. → [`soluciones/ej2_intercambiar.cpp`](soluciones/ej2_intercambiar.cpp)

3. **`sumaArreglo` genérico.** Plantilla `T sumaArreglo(T arr[], int n)` que sume los elementos.
   → [`soluciones/ej3_suma.cpp`](soluciones/ej3_suma.cpp)

4. **Reto — Par genérico.** Plantilla de clase `Par<A, B>` con dos valores de tipos distintos y un
   método `mostrar()`. → [`soluciones/ej4_par.cpp`](soluciones/ej4_par.cpp)
