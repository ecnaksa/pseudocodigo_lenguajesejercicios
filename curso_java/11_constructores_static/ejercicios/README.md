# Ejercicios — Lección 11

1. **Fraccion.** Clase `Fraccion` con `numerador` y `denominador`, constructor (denominador 1 por
   defecto usando sobrecarga), método `valor()` (double) y `toString()` (formato `n/d`).
   → [`soluciones/Fraccion.java`](soluciones/Fraccion.java)

2. **CuentaBancaria con constructor.** Reescribe la cuenta para que el saldo inicial se pase en el
   **constructor**. → [`soluciones/CuentaBancaria.java`](soluciones/CuentaBancaria.java)

3. **Libro.** Clase `Libro` con `titulo`, `autor`, `paginas`, constructor completo y `toString()`.
   → [`soluciones/Libro.java`](soluciones/Libro.java)

4. **Reto — instancias vivas.** Clase `Objeto` con un `static int vivos` que se incremente en el
   constructor. Crea varios y muestra el total. → [`soluciones/Objeto.java`](soluciones/Objeto.java)
