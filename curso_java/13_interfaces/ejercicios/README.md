# Ejercicios — Lección 13

1. **Interfaz Figura.** Interfaz `Figura` con `double area()`; clases `Circulo` y `Cuadrado` que la
   implementen. Recórrelas polimórficamente.
   → [`soluciones/Figuras.java`](soluciones/Figuras.java)

2. **Interfaz Sonoro.** Interfaz `Sonoro` con `String sonido()`; `Perro` y `Campana` que la
   implementen. → [`soluciones/Sonoros.java`](soluciones/Sonoros.java)

3. **Reto — ordenar productos.** Clase `Producto` (nombre, precio) que implemente
   `Comparable<Producto>` ordenando por precio; ordena un arreglo con `Arrays.sort`.
   → [`soluciones/Productos.java`](soluciones/Productos.java)
